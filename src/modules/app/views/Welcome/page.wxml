<block>
  <navgation show="{{false}}" />
  <view wx:if="{{inited}}">
    <image bindload="onComplete" class="ad" mode="aspectFill" src="{{clientPublishPath + imageUrl}}"></image>
    <van-button class="timer" round type="info" bind:click="onSkip">{{countdown}} 跳过</van-button>
  </view>
</block>
